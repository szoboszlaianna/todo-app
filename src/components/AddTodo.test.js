import { render, screen, fireEvent } from '@testing-library/react';
import TodoItem from './TodoItem';

describe('AddTodo test', () => {
  test('Check if form is submited', () => {
    //todo
  });
});
